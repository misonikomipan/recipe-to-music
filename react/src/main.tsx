import React from 'react'
import ReactDOM from 'react-dom/client'
import Cookingidol from './pages/CookingIdol.tsx'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <Cookingidol />
  </React.StrictMode>,
)
